# Plan 22-01: Consumer Credit Risk Collector + Dashboard Panel

**Created**: 2026-02-20  
**Wave**: 1  
**Effort**: M (Medium)  
**Priority**: HIGH

## Goal

Implementare monitoraggio completo del consumer credit risk e portarlo nel dashboard principale.

## Deliverables

1. Estensione `FREDCollector` con serie consumer credit stress
2. Nuovo `ConsumerCreditRiskCollector`
3. Nuovo componente dashboard `consumer_credit` con:
   - chart `XLP/XLY`
   - chart `AXP vs IGV`
   - credit metrics
   - tabella stock sensitivity
4. Wiring in layout/callback
5. Test unitari dedicati

## Tasks

### Task 1: Extend FRED series map

Add in `src/liquidity/collectors/fred.py`:
- `HCCSDODNS` (consumer credit outstanding, millions)
- `SLOASM` (student loans, millions)
- `DRALACBS` / `CORALACBS` (all-loan delinquency/charge-off rates)
- `DRSFRMACBS` / `CORSFRMACBS` (mortgage delinquency/charge-off rates)
- `QBPBSTASTLNLESSRES` (loan loss reserves)

### Task 2: Create ConsumerCreditRiskCollector

Create `src/liquidity/collectors/consumer_credit_risk.py` with:
- `collect_credit_risk()` for macro credit stress series
- `collect_market_prices()` for `XLP`, `XLY`, `AXP`, `IGV` + stock universe
- `build_tracking_series()`:
  - `consumer_credit_ex_students_b`
  - `debt_in_default_est_b`
  - `loan_loss_reserves_b`
  - `usd_liquidity_index`
- `calculate_xlp_xly_ratio()`
- `calculate_axp_igv_relative()`
- `rank_credit_sensitive_stocks()`

### Task 3: Add dashboard component

Create `src/liquidity/dashboard/components/consumer_credit.py`:
- `create_consumer_credit_panel()`
- `create_xlp_xly_ratio_chart()`
- `create_axp_igv_spread_chart()`
- `create_consumer_credit_metrics()`
- `create_sensitive_stocks_table()`

### Task 4: Integrate into dashboard wiring

Update:
- `src/liquidity/dashboard/layout.py`
- `src/liquidity/dashboard/callbacks_main.py`
- `src/liquidity/dashboard/components/__init__.py`

### Task 5: Tests

Create/update:
- `tests/unit/collectors/test_consumer_credit_risk.py`
- `tests/unit/test_dashboard/test_components/test_consumer_credit.py`
- `tests/unit/test_dashboard/test_layout.py`

## Validation

- [x] `ruff check` clean
- [x] `py_compile` clean
- [ ] full `pytest` (blocked in sandbox by `/dev/urandom` runtime limitation)

## Notes

- Implementazione completata nel commit: `9a229d7`
- Push completato su `main` (origin)

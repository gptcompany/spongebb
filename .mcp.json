{
  "mcpServers": {
    "claude-flow": {
      "command": "npx",
      "args": ["-y", "claude-flow@latest", "mcp", "start"]
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    },
    "n8n-mcp": {
      "command": "bash",
      "args": [
        "-c",
        "eval \"$(sops --input-type dotenv --output-type dotenv -d /media/sam/1TB/.env.enc 2>/dev/null | grep N8N_API | sed 's/^/export /')\" && npx -y n8n-mcp"
      ],
      "env": {
        "N8N_API_URL": "https://n8nubuntu.princyx.xyz/",
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "warning"
      }
    },
    "grafana": {
      "command": "bash",
      "args": [
        "-c",
        "eval \"$(sops --input-type dotenv --output-type dotenv -d /media/sam/1TB/.env.enc 2>/dev/null | grep GRAFANA | sed 's/^/export /')\" && /home/sam/go/bin/mcp-grafana"
      ]
    }
  }
}

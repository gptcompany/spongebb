# Plan 22-03: Direct Container Runtime Implementation (Dashboard + Python/E2E Tests)

**Created**: 2026-02-20  
**Wave**: 3  
**Effort**: M (Medium)  
**Priority**: HIGH

## Goal

Implementare il workflow operativo `container diretto` definito nel plan 22-02, con comandi standardizzati per:
- avvio dashboard,
- test Python runtime,
- regressione visuale Playwright.

## Deliverables

1. Docker image split:
   - `runtime` (produzione)
   - `test-runtime` (dipendenze dev incluse)
2. Docker Compose services:
   - `liquidity-dashboard`
   - `liquidity-dashboard-dev`
   - `liquidity-dashboard-test` (deterministico)
   - `liquidity-pytest`
   - `liquidity-playwright`
3. `Makefile` con target operativi
4. README con runbook step-by-step

## Tasks

### Task 1: Dockerfile targets

- Introduzione di stage `builder-prod`, `builder-test`
- Aggiunta stage `test-runtime` per eseguire pytest in container

### Task 2: Compose runtime/test topology

- Aggiunta servizio dashboard per runtime quotidiano
- Aggiunta servizio dashboard deterministico per visual tests
- Aggiunta servizio Playwright containerizzato per regression checks
- Aggiunta servizio pytest runtime in container

### Task 3: Operational UX

- Creazione `Makefile` con target:
  - `build`, `up`, `up-dev`, `down`, `logs`, `ps`
  - `test-python`, `test-visual`, `test-visual-update`
- Aggiornamento README con sequenze eseguibili step-by-step

## Validation

- [x] `docker compose config` valido
- [x] `make help` espone i target previsti
- [x] documentazione runtime allineata a servizi e target effettivi

## Notes

- Obiettivo: ridurre dipendenza da runtime sandbox e rendere i test riproducibili.

---
phase: 03-overnight-rates-fx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/liquidity/collectors/sofr.py, tests/integration/test_sofr_collector.py, src/liquidity/collectors/__init__.py]
autonomous: true
---

<objective>
Implement SOFR (Secured Overnight Financing Rate) collector with NY Fed primary API and FRED fallback.

Purpose: SOFR is the primary USD overnight rate (replaced LIBOR). Essential for carry trade signals and funding stress detection.
Output: Working SOFRCollector with multi-tier fallback, integration tests, registered in collector registry.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-overnight-rates-fx/03-RESEARCH.md

# Existing patterns to follow:
@src/liquidity/collectors/base.py
@src/liquidity/collectors/boc.py
@src/liquidity/collectors/boe.py
@src/liquidity/collectors/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SOFR collector with NY Fed primary and FRED fallback</name>
  <files>src/liquidity/collectors/sofr.py</files>
  <action>
Create SOFRCollector extending BaseCollector[pd.DataFrame].

**Primary source (Tier 1): NY Fed Markets API**
- Endpoint: `https://markets.newyorkfed.org/api/rates/secured/sofr/last/{n}.json`
- Use `last/30.json` for historical, `last/1.json` for latest
- Parse response: `data["refRates"][i]` contains:
  - `effectiveDate`: "2026-01-21"
  - `percentRate`: 3.63 (already in percent units)
  - `volumeInBillions`: 3087

**Fallback (Tier 2): FRED via OpenBB**
- Series: `SOFR`
- Use existing FredCollector pattern with asyncio.to_thread()

**Guaranteed fallback (Tier 3): Cached baseline**
- BASELINE_VALUE = 4.35 (percent, Jan 2026)
- BASELINE_DATE = "2026-01-22"

**Standard output columns:** timestamp, series_id, source, value, unit
- series_id: "SOFR"
- source: "nyfed" | "fred" | "cached_baseline"
- unit: "percent"

Follow boe.py multi-tier fallback pattern. Register with registry.register("sofr", SOFRCollector).
  </action>
  <verify>python -c "from liquidity.collectors.sofr import SOFRCollector; print('Import OK')"</verify>
  <done>SOFRCollector created with NY Fed primary, FRED fallback, cached baseline. Registered in collector registry.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for SOFR collector</name>
  <files>tests/integration/test_sofr_collector.py</files>
  <action>
Create integration tests following test_fred_collector.py pattern.

**Tests:**
1. `test_sofr_collector_nyfed_api`: Test NY Fed API directly (always works, no auth)
2. `test_sofr_collector_fallback`: Mock NY Fed failure, verify FRED fallback works
3. `test_sofr_collector_baseline`: Mock all failures, verify cached baseline returned

**Test assertions:**
- DataFrame has columns: timestamp, series_id, source, value, unit
- series_id == "SOFR"
- value is float in reasonable range (0-10 percent)
- unit == "percent"

Use pytest.mark.asyncio for async tests. Use httpx_mock or unittest.mock for mocking.
  </action>
  <verify>uv run pytest tests/integration/test_sofr_collector.py -v --tb=short</verify>
  <done>Integration tests pass for all three tiers (NY Fed, FRED fallback, cached baseline).</done>
</task>

<task type="auto">
  <name>Task 3: Register and export SOFR collector</name>
  <files>src/liquidity/collectors/__init__.py</files>
  <action>
Add SOFRCollector to __init__.py exports:
1. Add import: `from liquidity.collectors.sofr import SOFRCollector`
2. Add to __all__ list: "SOFRCollector"

Verify registration by checking registry.get("sofr") returns the collector class.
  </action>
  <verify>python -c "from liquidity.collectors import SOFRCollector, registry; print(registry.get('sofr'))"</verify>
  <done>SOFRCollector exported from package and registered in collector registry.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run python -c "from liquidity.collectors.sofr import SOFRCollector"` succeeds
- [ ] `uv run pytest tests/integration/test_sofr_collector.py -v` passes
- [ ] NY Fed API call returns valid data (no auth required)
- [ ] FRED fallback works (with LIQUIDITY_FRED_API_KEY configured)
- [ ] Cached baseline returns valid DataFrame when all sources fail
</verification>

<success_criteria>

- SOFRCollector implemented with 3-tier fallback
- All integration tests pass
- Collector registered and exported
- No TypeScript/linting errors
  </success_criteria>

<output>
After completion, create `.planning/phases/03-overnight-rates-fx/03-01-SUMMARY.md`
</output>

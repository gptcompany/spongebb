# Plan 25-01: Provider Scaffold & Registration

## Goal
Create the Provider object, entry point registration, and base model infrastructure so OpenBB discovers `obb.liquidity.*`.

## Tasks

### T1: Create models package
**File:** `src/liquidity/openbb_ext/models/__init__.py`
```python
"""OpenBB Provider models for liquidity data."""
```

### T2: Create Provider registration
**File:** `src/liquidity/openbb_ext/provider.py`
```python
from openbb_core.provider.abstract.provider import Provider

from liquidity.openbb_ext.models.net_liquidity import LiquidityNetLiquidityFetcher
from liquidity.openbb_ext.models.global_liquidity import LiquidityGlobalLiquidityFetcher
from liquidity.openbb_ext.models.stealth_qe import LiquidityStealthQEFetcher

liquidity_provider = Provider(
    name="liquidity",
    website="https://github.com/gptcompany/spongebb",
    description="Global liquidity monitoring: Fed Net Liquidity (Hayes), Global CB aggregate, Stealth QE detection.",
    credentials=[],
    fetcher_dict={
        "LiquidityNetLiquidity": LiquidityNetLiquidityFetcher,
        "LiquidityGlobalLiquidity": LiquidityGlobalLiquidityFetcher,
        "LiquidityStealthQE": LiquidityStealthQEFetcher,
    },
)
```

### T3: Add entry point to pyproject.toml
Add under `[project.entry-points]`:
```toml
[project.entry-points."openbb_provider_extension"]
liquidity = "liquidity.openbb_ext.provider:liquidity_provider"
```

### T4: Verify entry point discovery
```bash
uv pip install -e .
python -c "from openbb_core.app.extension_loader import ExtensionLoader; el = ExtensionLoader(); print([e.name for e in el.provider_objects])"
```

## Files Modified
- `pyproject.toml` (add entry-points section)
- `src/liquidity/openbb_ext/models/__init__.py` (new)
- `src/liquidity/openbb_ext/provider.py` (new)

## Dependencies
- None (first plan)

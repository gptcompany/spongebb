---
phase: 08-analysis-correlations
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified: [src/liquidity/analyzers/alert_engine.py, tests/integration/test_analyzers.py]
autonomous: true
---

<objective>
Implement the Alert Engine and integration tests for the complete Phase 8 analyzer suite.

Purpose: The alert engine detects correlation regime shifts (>0.3 change or >2σ statistical deviation) and prepares alert payloads for Discord (Phase 10). Integration tests verify the full analyzer pipeline works end-to-end.

Output: `AlertEngine` class for regime shift detection, plus integration tests covering RegimeClassifier + CorrelationEngine working together.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-analysis-correlations/08-CONTEXT.md
@.planning/phases/08-analysis-correlations/08-RESEARCH.md

# Depends on 08-01 and 08-02 outputs
@src/liquidity/analyzers/regime_classifier.py (from 08-01)
@src/liquidity/analyzers/correlation_engine.py (from 08-02)

# Pattern to follow
@src/liquidity/calculators/stealth_qe.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AlertEngine</name>
  <files>src/liquidity/analyzers/alert_engine.py</files>
  <action>
    Create AlertEngine class for detecting regime and correlation shifts:

    **Enum: AlertType**
    - REGIME_SHIFT = "REGIME_SHIFT"
    - CORRELATION_BREAKDOWN = "CORRELATION_BREAKDOWN"
    - CORRELATION_SURGE = "CORRELATION_SURGE"

    **Enum: AlertSeverity**
    - CRITICAL = "CRITICAL" (regime change + correlation breakdown together)
    - HIGH = "HIGH" (regime change OR major correlation shift)
    - MEDIUM = "MEDIUM" (moderate correlation change)
    - LOW = "LOW" (minor statistical deviation)

    **Dataclass: Alert**
    - timestamp: datetime
    - alert_type: AlertType
    - severity: AlertSeverity
    - title: str (e.g., "Regime Shift: EXPANSION → CONTRACTION")
    - message: str (detailed description)
    - asset: str | None (for correlation alerts)
    - previous_value: float | None
    - current_value: float | None
    - change: float | None
    - z_score: float | None (statistical deviation)
    - metadata: dict[str, Any] (extra context for Discord embed)

    **Class: AlertEngine**
    - CORRELATION_THRESHOLD = 0.3 (absolute change)
    - SIGMA_THRESHOLD = 2.0 (statistical deviation)
    - ROLLING_WINDOW = 90 (for mean/std calculation)

    **Methods:**
    - __init__(self, settings: Settings | None = None)
    - check_regime_shift(self, current: RegimeResult, previous: RegimeResult | None) -> Alert | None
      - Returns alert if direction changed
      - Severity: HIGH for direction change, MEDIUM for intensity change >20
    - check_correlation_shift(self, correlations: pd.Series, asset: str) -> Alert | None
      - Detects correlation breakdown using dual threshold (from RESEARCH.md):
        1. Absolute change > CORRELATION_THRESHOLD
        2. OR z-score > SIGMA_THRESHOLD from rolling mean
      - Returns CORRELATION_BREAKDOWN if correlation dropped, CORRELATION_SURGE if increased
    - check_all(self, regime: RegimeResult, correlations: dict[str, pd.Series], previous_regime: RegimeResult | None) -> list[Alert]
      - Run all checks, return list of alerts (may be empty)
    - format_discord_payload(self, alert: Alert) -> dict
      - Format alert for Discord webhook (embed structure)
      - Color coding: RED for CRITICAL, ORANGE for HIGH, YELLOW for MEDIUM, BLUE for LOW

    **Algorithm for correlation shift (from RESEARCH.md):**
    ```python
    def detect_correlation_shift(correlations: pd.Series) -> bool:
        if len(correlations) < ROLLING_WINDOW + 1:
            return False

        current = correlations.iloc[-1]
        previous = correlations.iloc[-2]
        rolling_mean = correlations.rolling(ROLLING_WINDOW).mean().iloc[-2]
        rolling_std = correlations.rolling(ROLLING_WINDOW).std().iloc[-2]

        # Absolute change check
        absolute_change = abs(current - previous)

        # Statistical deviation check
        z_score = abs(current - rolling_mean) / rolling_std if rolling_std > 0 else 0

        return absolute_change > CORRELATION_THRESHOLD or z_score > SIGMA_THRESHOLD
    ```

    **Anti-patterns to avoid:**
    - DON'T alert on every fluctuation - require dual threshold (absolute + statistical)
    - DON'T fire alerts without previous state context
    - DON'T include raw floats in Discord messages - format with 2 decimals

    Use logging (logger = logging.getLogger(__name__))
  </action>
  <verify>ruff check src/liquidity/analyzers/alert_engine.py passes</verify>
  <done>AlertEngine implemented with dual-threshold detection</done>
</task>

<task type="auto">
  <name>Task 2: Update analyzers __init__.py with alert exports</name>
  <files>src/liquidity/analyzers/__init__.py</files>
  <action>
    Update the analyzers module __init__.py to include all Phase 8 exports:

    Add imports:
    - from liquidity.analyzers.alert_engine import (
        AlertEngine,
        Alert,
        AlertType,
        AlertSeverity,
    )

    Update __all__ list to include all Phase 8 exports:
    - RegimeClassifier, RegimeResult, RegimeDirection (from 08-01)
    - CorrelationEngine, CorrelationResult, CorrelationMatrix (from 08-02)
    - AlertEngine, Alert, AlertType, AlertSeverity (from 08-03)

    Update module docstring to explain:
    - Regime classification for trading signals
    - Correlation tracking for asset relationships
    - Alert detection for regime shifts
  </action>
  <verify>python -c "from liquidity.analyzers import AlertEngine, Alert, AlertType" succeeds</verify>
  <done>All Phase 8 classes exported from analyzers module</done>
</task>

<task type="auto">
  <name>Task 3: Create integration tests for analyzers</name>
  <files>tests/integration/test_analyzers.py</files>
  <action>
    Create integration tests for the full analyzer pipeline:

    **Test cases:**
    1. test_regime_classifier_with_mock_data: Mock calculator results, verify regime output
    2. test_correlation_engine_with_synthetic_prices: Generate synthetic price series, verify correlations
    3. test_alert_engine_regime_shift: Create two RegimeResults with different directions, verify alert
    4. test_alert_engine_correlation_breakdown: Create correlation series with sudden drop, verify alert
    5. test_alert_engine_no_alert_minor_change: Small correlation change should not trigger alert
    6. test_full_pipeline: Mock calculators → RegimeClassifier → CorrelationEngine → AlertEngine

    **Fixtures:**
    - mock_net_liquidity_series: pd.Series with synthetic net liquidity values
    - mock_global_liquidity_series: pd.Series with synthetic global liquidity values
    - mock_stealth_qe_results: List of StealthQEResult with varying scores
    - mock_asset_prices: pd.DataFrame with BTC, SPX, GOLD prices (synthetic)

    Use pytest markers:
    - @pytest.mark.integration for full pipeline tests
    - @pytest.mark.asyncio for async tests (if using async calculators)

    Don't require actual API keys - use mocked data throughout.
  </action>
  <verify>uv run pytest tests/integration/test_analyzers.py -v passes</verify>
  <done>Integration tests pass for full analyzer pipeline</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ruff check src/liquidity/analyzers/` passes
- [ ] `uv run pytest tests/integration/test_analyzers.py -v` passes
- [ ] AlertEngine uses dual threshold (0.3 absolute + 2σ statistical)
- [ ] Discord payload formatter included
- [ ] All Phase 8 classes exported from analyzers module
</verification>

<success_criteria>

- AlertEngine detects regime shifts and correlation breakdowns
- Dual-threshold algorithm prevents alert fatigue
- Discord payload format ready for Phase 10
- Integration tests verify full pipeline
- All analyzers exported from module
</success_criteria>

<output>
After completion, create `.planning/phases/08-analysis-correlations/08-03-SUMMARY.md`
</output>

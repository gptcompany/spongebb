---
phase: 05-capital-flows-stress
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/liquidity/collectors/risk_etfs.py
  - tests/integration/test_risk_etfs.py
  - src/liquidity/collectors/__init__.py
autonomous: true
---

<objective>
Create RiskETFCollector for risk sentiment ETF flows (SPY, TLT, HYG).

Purpose: Track real-time risk-on/risk-off sentiment via major ETF flows. SPY (equity risk), TLT (flight to safety), HYG (credit risk appetite). Complements Phase 4 commodity ETFs.

Output: RiskETFCollector using yfinance (following etf_flows.py pattern).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing ETF pattern to follow:
@src/liquidity/collectors/etf_flows.py
@src/liquidity/collectors/fx.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RiskETFCollector for sentiment ETFs</name>
  <files>src/liquidity/collectors/risk_etfs.py</files>
  <action>
Create RiskETFCollector following existing etf_flows.py pattern.

**ETF Mapping:**
```python
RISK_ETF_TICKERS: dict[str, str] = {
    "SPY": "SPY",    # S&P 500 (equity risk)
    "TLT": "TLT",    # 20+ Year Treasury (flight to safety)
    "HYG": "HYG",    # High Yield Corporate (credit risk appetite)
    "IEF": "IEF",    # 7-10 Year Treasury (duration positioning)
    "LQD": "LQD",    # Investment Grade Corporate (credit quality)
}

RISK_ETF_TYPE: dict[str, str] = {
    "SPY": "equity",
    "TLT": "treasury_long",
    "HYG": "high_yield",
    "IEF": "treasury_mid",
    "LQD": "investment_grade",
}
```

**Methods to implement (follow etf_flows.py):**
- `collect_current_shares()` -> Shares outstanding, AUM, NAV for each ETF
- `collect_historical_prices()` -> Batch yf.download for all tickers
- `estimate_daily_flows()` -> Share changes over period
- `collect_equity_etfs()` -> SPY only
- `collect_bond_etfs()` -> TLT, IEF, HYG, LQD
- `collect_all()` -> All risk ETFs
- `collect()` -> Main entry (all ETFs)
- `calculate_risk_appetite()` -> Ratio of SPY/TLT flows (risk-on vs risk-off)

**Output format (match etf_flows.py):**
- For shares: timestamp, etf, risk_type, source, shares_outstanding, total_assets, nav_price
- For prices: timestamp, etf, risk_type, source, close, volume
- source: "yfinance"

Register as "risk_etfs" in registry.
  </action>
  <verify>python -c "from liquidity.collectors.risk_etfs import RiskETFCollector; print('Import OK')"</verify>
  <done>RiskETFCollector imports without error</done>
</task>

<task type="auto">
  <name>Task 2: Create Risk ETF tests</name>
  <files>tests/integration/test_risk_etfs.py, src/liquidity/collectors/__init__.py</files>
  <action>
Create integration tests for RiskETFCollector (follow test_etf_flows.py pattern).

**Tests to create:**
1. test_collect_current_shares - All 5 ETFs have shares data
2. test_collect_current_shares_spy_valid - SPY shares > 500M (huge ETF)
3. test_collect_historical_prices - Batch download works
4. test_historical_prices_no_nan - Forward fill handles gaps
5. test_collect_equity_etfs - Returns SPY only
6. test_collect_bond_etfs - Returns TLT, IEF, HYG, LQD
7. test_estimate_daily_flows - Share change calculation
8. test_risk_appetite_ratio - SPY/TLT calculation
9. test_output_format_shares - Correct columns for shares
10. test_output_format_prices - Correct columns for prices
11. test_registry_integration - Registered as "risk_etfs"
12. test_etf_type_mapping - risk_type column populated

**Update __init__.py:**
- Add RiskETFCollector export
- Add RISK_ETF_TICKERS, RISK_ETF_TYPE constants

Mark tests with @pytest.mark.integration.
  </action>
  <verify>cd /media/sam/1TB/spongebb && uv run pytest tests/integration/test_risk_etfs.py -v --tb=short 2>&1 | tail -30</verify>
  <done>All Risk ETF tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/integration/test_risk_etfs.py -v` passes
- [ ] `uv run python -c "from liquidity.collectors import RiskETFCollector; print('OK')"` works
- [ ] No pyright errors: `uv run pyright src/liquidity/collectors/risk_etfs.py`
</verification>

<success_criteria>
- RiskETFCollector fetches SPY, TLT, HYG, IEF, LQD data
- Risk appetite ratio calculation works
- All tests pass
- Registered as "risk_etfs" in collector registry
</success_criteria>

<output>
After completion, create `.planning/phases/05-capital-flows-stress/05-04-SUMMARY.md`
</output>
